# 并发开发与串行验收

适用于本原型的多 agent 设计、开发与恢复。页面要求仍取 `catalog.json` 及四份原文，公式取 [数据契约](data-contract.md)，正式证据取 [Loop 协议](loop-protocol.md)。本协议增加开发分工，不放宽页面完成条件。

## 角色与执行边界

- **协调者**：维护集成目录与全局状态，确认接口、分配任务、合入、检查、浏览器验收和最终交付。
- **开发者**：在分配的工作目录完成一个 1–5 页业务包，或一个明确公共能力；自检后交付。任务完成后向协调者要下一包，不自行扩大范围或创建更多开发 agent。
- **审查者**：可临时占用一个空闲席位，只读检查代码、依赖或证据。用户只要求设计时，只委派设计/审查任务。

并发数由运行环境决定，建议一个协调者加最多三个开发者。没有委派工具时由单 agent 顺序执行，不声称后台存在开发者。原型开发与真实部署、消息发送等外部动作的授权边界不变。

## 工作目录与共享文件

1. 有 Git 时优先使用集成目录之外、权限允许位置的独立 worktree，每个开发者独立分支。先检查未提交修改；起点必须包含已确认公共接口，记录提交号。不能假定新 worktree 包含用户未提交内容。
2. 委派时给出绝对 `workdir`，开发者先核对 `pwd` 与分支；每次 shell/测试操作显式使用自己的 `workdir`，文件编辑也使用对应绝对路径。子 agent 默认共享主目录，不能直接按默认目录动手。
3. 不在集成目录下建 `.worktrees` 等嵌套副本：这些文件会影响全项目指纹和工程扫描。不把源码移到 `skills/.agents/.codex/.pms-loop` 等排除目录来规避校验。
4. 目录、Git 元数据、网络需要环境批准时使用实际权限流程；不能为了回避审批换工具。若暂时无法隔离，先继续只读设计或串行实现。共享目录并发写仅可在明确互不重叠的文件分工下使用，且正式验收前全体停止写入。
5. 共享文件实行单写者：全局状态结构、Mock 初始化、公共类型、selectors、规则接口、路由聚合、主题及工程配置只由协调者修改。开发者提出接口需求，协调者先发布可用接口提交，再由开发者同步实现独占模块。跨域消费者默认归协调者；确需转交时先确认旧负责人停止写入并更新文件归属。
6. 同一接口提案至少说明新增字段/动作、来源版本、读写对象、已有调用影响和测试场景。协调者处理兼容默认值、既有数据迁移与回归，优先向后兼容扩展，避免各分支重复改造公共框架。

## 分支与交付范围检查

1. 每包在分配单记录精确 `owned_paths` 数组（文件路径或目录 glob），不能仅写“对应业务域”。共享文件不属于开发者白名单。
2. 协调者发布 `contract_commit` / `base_commit` 后，开发者从这个提交新建任务分支；旧分支/WIP 保留，不 reset、不覆盖。已经在开发的包先固化 WIP，再将尚未交付的独占提交迁移到新任务分支。旧协议形成的共享补丁只供协调者一次性审查接入，不继续叠加。
3. 开发者不把其他 agent 的分支或主分支反复 merge 到交付分支；协调者只从任务起点到交付终点单向 cherry-pick 一次，并记录原提交与集成提交映射。再次交付仅包含上次终点之后的新提交。
4. 合入前运行范围检查，发现越界先拆分交付或明确唯一负责人，不自动扩大白名单。该检查只验证提交范围，不能代替代码审查、冲突检查和业务测试。

```bash
python3 skills/pms-prototype-loop/scripts/check_delivery.py \
  --workdir /absolute/developer/worktree --base BASE --head HEAD \
  --allow 'src/pages/settlement/PostEvaluationPage.tsx' \
  --allow 'src/mock/closeout.ts' --allow 'src/models/closeout.ts' \
  --allow 'src/models/__tests__/closeout.test.ts'
```

`business.ts` 逐包保持薄的组合入口：新模块导出状态类型、初始化函数和动作处理器；业务校验留在本域。拆分时保持单一 store、纯 transition 与现有领域行为，不同时重写所有模块。

## 分配单与交付单

协调者在集成目录 `.pms-loop/parallel/<包ID>/assignment.json` 记录分配信息，在同目录 `delivery.json` 保存交付和合入记录。它只记录调度，不是第二份页面验收状态；`.pms-loop/state.json` 仍是唯一完成事实。设计阶段不必建立这些运行时文件。

分配单需包含：

| 字段 | 内容 |
|---|---|
| `package_id / items / owner` | 包ID、精确页面ID、当前负责 agent |
| `integration_root / workdir / branch / base_commit` | 集成与开发目录、分支、共同起点 |
| `contract_commit` | 本包依赖的公共接口提交号 |
| `owned_paths / shared_changes` | 独占路径、已确认共享改动清单 |
| `development_dependencies / acceptance_dependencies` | 开始实现与可正式验收的前置条件，分开记录 |
| `scenarios / fixtures` | 正常、阻断、角色、跨页场景，真实对象ID或创建步骤 |
| `status / next_action` | `assigned / submitted / integrated / needs_revision / blocked` 与准确下一步；页面是否done只读正式状态 |
| `writer / last_confirmed_at` | 活跃写入者agent标识及最近核实时间；恢复时仍须查询实际活动，不能因记录过期直接转派目录 |

开发者交付：起点与最终提交号（或相对起点的补丁）、实际改动路径、共享改动说明、路由/真实ID/角色、测试命令与退出码、观察记录和证据位置、未解决依赖。不能只报告“完成了几个文件”。协调者检查 diff 的范围与完整性后才合入。

`delivery.json` 至少记录 `base_commit`、不可变的 `delivery_commit`（补丁交付则为 `patch_path/patch_sha256`）、`depends_on_deliveries`（尚未合入的上游包及交付提交）、`shared_changes`、`checks`、`artifacts` 和 `remaining_dependencies`。共享接口分别标记 `proposed/confirmed/integrated`，只有实际集成后才填 `contract_commit`。合入后记录 `integration_commit`；未提交的集成结果记录实际diff及源码指纹，不虚构提交号。正式轮次建立后记录 `formal_rounds`，仅作为证据索引。

A交付后可在原worktree接下一包，先固定旧包交付提交/补丁，再记录新包的起点和上游依赖；不能让旧交付随着分支继续推进而漂移。已有记录缺字段时依据Git与实际活动补齐，不把记录格式当成额外阻塞。

需要跨包功能时，开发者报告具体依赖；可先实现已有数据支持的部分，交付为“待集成/待验收”，继续独立工作。**依赖未完成时不能隐藏原文要求的按钮、用提示替代下钻，或填假来源数据来获得验收。**

## 集成流程

```text
公共接口及分配单
  ├─ GS 工作目录：实现 → 自检 → 交付 ─┐
  ├─ YS 工作目录：实现 → 自检 → 交付 ─┼─ 协调者合入队列
  └─ JS 工作目录：实现 → 自检 → 交付 ─┘
       ↓（其他工作目录可继续下一包）
集成源码稳定 → 正式 begin → check → 浏览器观察/截图 → accept
```

- 独立功能可以先合入再等待下钻依赖。正式 `begin` 只选择已经具备验收前提的 1–5 项，避免长时间占住唯一轮次。已提前实现的页面仍按原文与 `plan.json` 完整核对。
- 协调者先实现并核对共享接口，再检查开发者交付只包含独占领域代码。只带入应用代码与确需的测试/文档，不合并工作副本中的正式 `.pms-loop`、`DEV_PROGRESS.md` 受管内容、临时入口及试验配置。
- 合入冲突逐处保留各包语义，不整体用某一侧覆盖共享文件。公共接口改变时通知仍在开发的 agent 更新起点、检查调用影响。
- 开发者自行运行真实的工程检查及相关领域测试；开发阶段可用独立浏览器做自检，但需明确端口、隔离测试状态并及时关闭创建的测试资源。可见的共享浏览器只由一个操作者使用。
- 正式 `check` 到 `accept` 期间冻结集成目录受指纹覆盖的所有输入，包含根目录说明、配置和 `docs/`；普通文档编辑也排队。`.lock` 只保护 Loop 命令，不会阻止其他程序改源码。
- 实际浏览器回归在集成版本进行。worker 的旧截图不直接充当集成版本证据，不能用新哈希包装旧观察。发现问题在集成目录修复后重跑本轮，或退回开发者修复、合入后重跑本轮；其他分支继续独立任务。
- 浏览器链路内保持状态，独立场景之间重置；记录演示日、角色及fixture，不同时操作同一个可变测试上下文。正式可见浏览器复用一个标签页，完成后清理本轮创建的资源。
- 共享能力兼容扩展（如新增实体/字段且旧fixture及不变量仍成立）做相关链路回归，不因触碰 `BusinessState` 文件就全量重开。页面行为失效时重开相应页面；已有基础不变量失效、基础能力需重新验收时重开对应FND，并服从控制器的全量重验规则。不能把破坏既有口径的改动称为兼容扩展来跳过验收。

每轮原有 `typecheck/lint/build/test:domain`、双宽度截图与证据规则保持。最终 `test:e2e`、72页覆盖、五条业务链与两条角色路线仍须在同一个集成版本通过。

## 依赖、恢复与完成

- 任务包是开发切分，正式轮次是验收切分，可以不同。包ID不使用 `R0001` 等正式轮次编号。
- 子包阻塞只更新其调度记录，协调者分派独立包；不要为尚未开始的正式页面调用 `defer`。已有正式轮次真实受阻时才按 Loop 协议处理。
- 会话恢复先读正式状态、活动轮次、分配单与各工作目录 Git 状态，核对已合入提交。不要重复创建相同分支、重新派给另一个仍有写入者的目录、覆盖未交付修改或重做已验收页面。
- 没有活动轮次时按集成和验收依赖选择；有活动轮次先恢复其检查/证据，不在后台追加合入。调度备注放 `.pms-loop/parallel`，重要恢复信息由协调者用 `note` 保存。
- 设计文档中的 22/72 等计数只是设计时快照，恢复时重新读取当前状态；新增包需覆盖剩余页且无重复负责人。
- 所有页经正式 `accept` 后由协调者进入 `FINAL`。并发任务全部交付不代表原型完成；记录开发、集成和正式验收三种进度，向用户报告实际验收数与剩余依赖。
