import { useActionAccess } from '@/hooks/useActionAccess';
import { canViewSensitiveField } from '@/mock/configuration-access';
import {configuredSubjects,selectCostRate,projectCostRegion} from '@/mock/configuration-finance';
import { useState } from 'react';
import { App, Button, Card, Col, Form, Input, InputNumber, Modal, Row, Select, Space, Table, Tabs } from 'antd';
import { useNavigate } from 'react-router-dom';
import { PageSection } from '@/components/common/PageSection';
import { StateView } from '@/components/common/StateView';
import { usePlanning } from './usePlanning';
import { PlanningHeader } from './PlanningLayout';
import { BudgetSummary, BudgetComparisonTable } from './BudgetSummary';
import { snapshotBudgetRate, getBudgetDraft, lineAmount, toBudgetVersion } from '@/mock/budget-drafts';
import { projectEstimate } from '@/mock/versions';
import type { BudgetDraft, BudgetLine } from '@/models/budget';
export function BudgetEditorPage(){
 const {canDo}=useActionAccess();
 const c=usePlanning(),{message,modal}=App.useApp(),navigate=useNavigate();const [local,setLocal]=useState<BudgetDraft>();const [editing,setEditing]=useState<string>();const [replacementAmount,setReplacementAmount]=useState<number>();const [form]=Form.useForm<BudgetLine>();const editingKind=Form.useWatch('kind',form);
 if(!c.project||!c.plan)return <StateView type="404"/>;if(!c.allowed||!['project-manager','pmo','finance','executive','admin'].includes(c.actor.role))return <StateView type="403"/>;const p=c.project;const showPersonalCost=['project-manager','pmo','finance','executive','admin'].includes(c.actor.role)&&canViewSensitiveField(c.data,c.actor,'labor-rate');const subjects=configuredSubjects(c.data,p.departmentId,p.type);const currentRate=(userId:string)=>selectCostRate(c.data,{userId,orgId:p.departmentId,projectType:p.type,region:projectCostRegion(c.data,p)}).hourlyYuan;const estimate=projectEstimate(p,c.data.estimates);if(!estimate)return <StateView type="empty" title="缺少项目绑定冻结概算"/>;
 const draft=local?.projectId===p.id?local:getBudgetDraft(c.data,p.id);const budget=toBudgetVersion(draft,c.actor);const canEdit=canDo('save-budget-draft',p.id)&&c.actor.role==='project-manager'&&c.actor.id===p.pmId&&!c.data.approvals.some(a=>a.projectId===p.id&&(a.status==='待审批'||a.kind==='budget'&&a.status==='通过'&&!a.baselineConfirmedAt));
 const save=()=>{try{if(!canEdit||!canDo('save-budget-draft',p.id))throw new Error('当前策略不允许保存预算');c.dispatch({type:'save-budget-draft',draft,expectedRevision:c.data.budgetDrafts[p.id]?.revision??0},c.actor);setLocal(undefined);message.success('预算草稿已保存');return true;}catch(e){message.error((e as Error).message);return false;}};
 const open=(line?:BudgetLine)=>{setReplacementAmount(undefined);form.resetFields();form.setFieldsValue(line??{id:`LINE-${p.id}-${crypto.randomUUID()}`,name:'',kind:'procurement',subjectId:'SUB-03',amount:0,plannedDays:1,travelDays:0,userId:p.pmId,grade:'项目岗位基准',taskId:c.plan?.tasks[0]?.id,stage:'开发实施',sourceEstimateItemId:'SUB-03',justification:'',department:p.departmentName,supplyMode:'采购'});setEditing(line?.id??'new');};
 const hiddenOriginalAmount=!showPersonalCost&&draft.lines.some(line=>line.id===editing&&line.kind==='labor');
 const columns=[{title:'预算事项',dataIndex:'name',width:170},{title:'WBS / 来源概算',width:170,render:(_:unknown,l:BudgetLine)=><>{c.plan?.tasks.find(t=>t.id===l.taskId)?.name??'独立费用'}<div>{l.sourceEstimateItemId?`${estimate.version} / ${l.sourceEstimateItemId}`:'新增测算项'}</div></>},{title:'测算依据',width:200,render:(_:unknown,l:BudgetLine)=>l.kind==='labor'?(showPersonalCost?`${l.plannedDays}人天 × 8小时 × ${(l.hourlyYuan??currentRate(l.userId??''))}元/小时`:`${l.plannedDays}人天 · 按冻结成本基准自动计算，人员单价已隐藏`):`${l.supplyMode} · ${l.stage}`},{title:'预算（万元）',width:120,align:'right' as const,render:(_:unknown,l:BudgetLine)=>l.kind==='labor'&&!showPersonalCost?'已隐藏':lineAmount(l).toFixed(2)},{title:'范围与调整说明',dataIndex:'justification',width:220},{title:'操作',width:140,fixed:'right' as const,render:(_:unknown,l:BudgetLine)=><Space><Button size="small" disabled={!canEdit} onClick={()=>open(l)}>编辑</Button><Button size="small" danger disabled={!canEdit} onClick={()=>setLocal({...draft,lines:draft.lines.filter(x=>x.id!==l.id)})}>删除</Button></Space>}];
 return <><PlanningHeader title="YS-09 项目预算编制" context={c}/><BudgetSummary budget={budget} estimate={estimate} income={p.revenueAmount??p.contractAmount}/><Card size="small" title={`预算草稿 R${draft.revision} · 引用概算 ${estimate.version}`} extra={<Space wrap><Button onClick={()=>{setLocal(undefined);navigate(`/projects/${p.id}/plan-review`);}}>取消 / 返回计划</Button><Button disabled={!canEdit} onClick={save}>保存草稿</Button><Button type="primary" disabled={!canEdit||!canDo('submit-budget-draft',p.id)} onClick={()=>modal.confirm({title:'确认保存并提交预算审批',content:`预算${budget.totalAmount.toFixed(2)}万元；提交后绑定当前概算、计划、资源和预算明细版本。`,onOk:()=>{if(!canEdit||!canDo('save-budget-draft',p.id)||!canDo('submit-budget-draft',p.id)){message.error('当前策略不允许保存并提交预算');return;}if(!save())return;try{c.dispatch({type:'submit-budget-draft',projectId:p.id},c.actor);navigate(`/projects/${p.id}/budget/review`);}catch(e){message.error((e as Error).message);}}})}>提交审批</Button></Space>}><Tabs items={[{key:'base',label:'项目与计划',children:<><p>客户：{p.customerName} · 项目收入：{p.revenueAmount??p.contractAmount}万元</p><p>范围：{c.plan.scope}</p><p>WBS {c.plan.tasks.length}项 / 里程碑 {c.plan.milestones.length}项 / 资源 {c.plan.resources.length}人</p><Button onClick={()=>navigate(`/projects/${p.id}/wbs`)}>核对WBS与资源</Button></>},...([['labor','交付人力'],['procurement','建设采购'],['expense','期间费用'],['third-party','第三方费用'],['outsource','外包预算'],['reserve','准备金']] as const).map(([kind,label])=>({key:kind,label,children:<><div style={{marginBottom:12}}><Button disabled={!canEdit} onClick={()=>{open();form.setFieldsValue({kind,subjectId:kind==='labor'?'SUB-01':kind==='outsource'?'SUB-02':kind==='procurement'?'SUB-03':kind==='reserve'?'SUB-05':'SUB-04-4'});}}>新增{label}</Button></div><Table rowKey="id" size="small" dataSource={draft.lines.filter(l=>l.kind===kind)} columns={columns} pagination={false} scroll={{x:1120}}/></>})),{key:'summary',label:'预算汇总与对比',children:<BudgetComparisonTable budget={budget} estimate={estimate}/>} ]}/></Card><PageSection title="预算调整说明" description="超概算时，提交审批须填写原因、措施和责任说明；草稿可随时保存。"><Row gutter={[16,16]}>{[['reason','超概算原因'],['mitigation','应对措施与毛利影响'],['responsibility','责任说明']] .map(([key,label])=><Col xs={24} xl={8} key={key}><label>{label}<Input.TextArea aria-label={label} disabled={!canEdit} rows={3} value={draft[key as 'reason']} onChange={e=>setLocal({...draft,[key]:e.target.value})}/></label></Col>)}</Row></PageSection>
 <Modal width={800} title="预算明细测算" open={!!editing} onCancel={()=>setEditing(undefined)} okButtonProps={{disabled:!canEdit}} onOk={()=>form.validateFields().then(values=>{const raw={...form.getFieldsValue(true),...values};if(hiddenOriginalAmount&&raw.kind!=='labor'){if(replacementAmount===undefined){message.error('转换为非人力事项后，请填写新的预算金额');return;}raw.amount=replacementAmount;}const original=draft.lines.find(l=>l.id===editing&&l.userId===raw.userId&&l.subjectId===raw.subjectId&&l.grade===raw.grade&&l.kind===raw.kind);const line=original?{...raw,hourlyYuan:original.hourlyYuan,rateVersion:original.rateVersion,expenseDailyYuan:original.expenseDailyYuan,expenseRateVersion:original.expenseRateVersion}:snapshotBudgetRate(c.data,p.id,raw);setLocal({...draft,lines:editing==='new'?[...draft.lines,line]:draft.lines.map(l=>l.id===editing?line:l)});setEditing(undefined);}).catch(()=>{})}><Form form={form} layout="vertical" disabled={!canEdit}><Row gutter={16}><Col span={12}><Form.Item name="name" label="预算事项" rules={[{required:true}]}><Input/></Form.Item></Col><Col span={12}><Form.Item name="kind" label="成本类型"><Select options={['labor','procurement','outsource','expense','third-party','reserve'].map((value,i)=>({value,label:['人力','采购','外包','期间费用','第三方费用','准备金'][i]}))}/></Form.Item></Col><Col span={12}><Form.Item name="subjectId" label="财务统一科目"><Select options={subjects.map(s=>({value:s.id,label:s.name}))}/></Form.Item></Col><Col span={12}><Form.Item name="taskId" label="关联WBS范围"><Select options={c.plan.tasks.map(t=>({value:t.id,label:t.name}))}/></Form.Item></Col><Col span={12}><Form.Item name="userId" label="人力成员（按成本基准计价）"><Select options={c.plan.resources.filter(r=>r.active).map(r=>({value:r.userId,label:showPersonalCost?`${r.name} · ${currentRate(r.userId)}元/小时`:r.name}))}/></Form.Item></Col><Col span={12}><Form.Item name="plannedDays" label="人力计划人天"><InputNumber min={0} precision={2}/></Form.Item></Col><Col span={12}><Form.Item name="travelDays" label="出差天数"><InputNumber min={0}/></Form.Item></Col>{editingKind!=='labor'&&<Col span={12}>{hiddenOriginalAmount?<Form.Item label="转换后的非人力预算金额（万元）" required><InputNumber aria-label="转换后的非人力预算金额" min={0} precision={2} value={replacementAmount} onChange={value=>setReplacementAmount(value??undefined)}/></Form.Item>:<Form.Item name="amount" label="非人力金额（万元，人力按基准自动计算）"><InputNumber min={0} precision={2}/></Form.Item>}</Col>}<Col span={12}><Form.Item name="stage" label="计划发生阶段"><Select options={['方案确认','开发实施','系统联调','内部初验','客户终验'].map(value=>({value,label:value}))}/></Form.Item></Col><Col span={12}><Form.Item name="supplyMode" label="供应方式"><Input/></Form.Item></Col><Col span={12}><Form.Item name="department" label="责任部门"><Input/></Form.Item></Col><Col span={12}><Form.Item name="sourceEstimateItemId" label="来源概算科目"><Select allowClear options={estimate.items.map(i=>({value:i.subjectId,label:i.subjectName}))}/></Form.Item></Col><Col span={24}><Form.Item name="justification" label="交付范围 / 调整原因"><Input.TextArea/></Form.Item></Col></Row></Form></Modal></>;
}
