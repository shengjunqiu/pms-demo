import type { ApprovalRoutingSnapshot, GradingVersion } from './configuration';
import type { UserRole } from '@/store/useAppStore';
import type { EstimateVersion } from './types';
export interface EarlyInvestmentInput { reason:string; amount:number; resourceTypes:string[]; department:string; people:string[]; startDate:string; endDate:string; signPlanDate:string; signPlan:string; riskLevel:'一般'|'高风险'; risks:string; exitPlan:string; estimateId:string; }
export interface EarlyInvestmentRequest extends EarlyInvestmentInput { id:string; opportunityId:string; status:'草稿'|'待审批'|'通过'|'驳回'; createdAt:string; createdBy:string; submittedAt?:string; estimateSnapshot?:EstimateVersion; requiredRole:'pmo'|'executive'; ruleVersion:string; ruleReason:string; opinion?:string; reviewedBy?:string; reviewedAt?:string; approvalSnapshot?:ApprovalRoutingSnapshot; gradingSnapshot?:GradingVersion; maximumCumulativeQuota?:number; approvalNode?:number; nodeEnteredAt?:string; nodeReviews?:{node:number;role:UserRole;by:string;approve:boolean;opinion:string;date:string}[]; }
export interface EarlyCostRecord { id:string; opportunityId:string; requestId:string; sourceId:string; subjectId:string; subjectName:string; type:'labor'|'procurement'|'outsource'|'expense'; amount:number; occurredDate:string; description:string; enteredBy:string; projectId?:string; ledgerId?:string; }
