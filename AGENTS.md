# 项目四算原型协作约定

## 任务范围与入口

- 本项目交付 React + Ant Design 高保真前端原型；页面、业务规则和验收以 `docs/` 中四份原文及其既定优先级为准。
- 使用仓库内的 [pms-prototype-loop](skills/pms-prototype-loop/SKILL.md)。若同时存在个人安装副本，本项目执行约定以仓库内版本为准。
- 开始或恢复工作先读 `DEV_PROGRESS.md`、`.pms-loop/state.json` 和当前代码。页面完成数只取正式验收状态，保留用户未提交修改。
- 用户只要求讨论、设计或审查时，完成该范围；不据此启动全量页面开发。本次并发设计与任务分配见 [并发开发方案](docs/PARALLEL_DEVELOPMENT.md)。

## 并发开发

- 用户授权多页持续开发时，按 [并发协议](skills/pms-prototype-loop/references/parallel-protocol.md) 将独立业务包委派给子 agent；建议一个协调者、最多三个开发者，服从实际可用席位。单页小改不必强行并发。
- 协调者负责集成目录、共享接口、任务分配、正式 Loop 和最终验收。开发者只操作被分配的工作目录和任务包，不能继续向下创建开发 agent。
- 子 agent 不自动拥有独立目录。委派前记录工作目录的绝对路径、分支、起点提交、文件归属、依赖和验收场景；调用 shell 工具时显式指定自己的 `workdir`。
- 优先在项目根目录之外的独立 Git worktree 开发。禁止在集成目录内嵌套工作副本，也不把应用源码放到指纹排除目录。目录、Git 和网络权限遵守实际环境限制。
- 共享文件实行单写者：`src/mock/business.ts`、`src/mock/index.ts`、`src/models/types.ts`、公共 selectors/规则、路由聚合与工程配置只由协调者修改；开发者仅提交接口说明，不在独立分支重复修改这些文件。跨域消费者也由协调者接入，或明确转交唯一文件负责人。
- 每包先约定精确 `owned_paths`，协调者先提交公共接口和统一同步点，开发者从该点建立新的任务分支；旧分支及 WIP 保留。交付只含该任务分支的独占提交，主分支单向 cherry-pick 一次，不将已回收的旧分支再 merge 回来。
- 合入前运行并发协议中的 `check_delivery.py`，拦截共享文件、越界路径和 merge 提交；业务语义仍由协调者审查。共享接口变更需要同步时，保存本包 WIP 后在新同步点建立分支，仅迁移尚未交付的独占提交。
- Mock 只保留一个权威业务状态；沿用 `useBusinessStore`、`transition` 和公共计算，不在页面新建一份项目、预算、审批或成本事实。

## 集成与验收

- 只有协调者可写集成目录的 `.pms-loop/state.json`、正式 `runs/` 和 `DEV_PROGRESS.md` 受管区块。开发者不在工作副本中运行 `init/begin/check/accept/reopen/reconcile/defer/note`。
- 子任务的“已交付/已集成”不等于页面已验收。正式 `begin/check/accept` 仍每轮 1–5 项，单活动轮次；开发任务不占用正式轮次。
- 正式 `check` 开始到截图、业务观察、`accept` 完成期间，冻结集成目录内全部受指纹覆盖的输入，包括源码、根目录说明及 `docs/`。其他 worktree 可以继续开发，合入排队。
- 独立分支的截图和测试只证明该分支；合入后在当前集成源码重新检查和观察，不能复制旧证据通过标记或只更新哈希。
- 浏览器正式验收由一个操作者复用一个标签页；每个独立测试场景重置演示状态，同一跨页链路保留状态，完成证据保存后关闭所创建的标签页。
- 改动影响已验收页面时做针对性回归，发现失效按 Loop 协议重开；源码或页面占位不能通过“降低验收标准”处理。

## 项目检查

- 使用现有 `pnpm-lock.yaml`。工程检查为 `pnpm typecheck`、`pnpm lint`、`pnpm build`、`pnpm test:domain`；最终交付还需补齐并执行真实的 `test:e2e`。
- 仅修改设计文档/技能时，校验技能资源与任务覆盖，不以应用验收轮次计数。不要为此改四份原文的指纹或重写历史验收记录。
