# PMS 平台设计规范

本规范以 React 18、Ant Design 5 和当前源码为准。业务规则、页面范围与数据口径仍以 docs 中四份原文及既定优先级为准。

## 设计原则

1. 优先回答当前角色的工作问题。项目经理先看待办与临期事项，领导先看经营指标、异常与决策，项目详情围绕同一个项目组织业务入口。
2. 首屏保留重要信息；低频筛选、辅助口径和原始资料可展开查看，不能删掉业务能力或丢失筛选上下文。
3. 数字、状态、颜色必须表达真实业务事实。“无预警”不等于“全部健康”，尚未结算不等于结算为零。
4. 以间距表达内容关系，避免连续嵌套多层卡片。可点击区域必须有清晰反馈和键盘访问能力。

## 视觉基础

主题入口：`src/theme/index.ts`；全局样式：`src/index.css`。

| 项目 | 当前规范 |
|---|---|
| 品牌主色 | #2563eb，用于主操作、链接和选中态 |
| 页面背景 / 内容背景 | #f8fafc / #ffffff |
| 主文字 / 辅助文字 | #0f172a / #64748b |
| 边框 | #e2e8f0 |
| 左侧导航 | 240px，折叠 64px；深色背景 #0f172a |
| 顶栏 | 64px；搜索、当前位置、数据日期、演示身份 |
| 正文字号 | 14px，密集表格 13px，辅助标签 12px |
| 页面标题 | 20px，业务名称优先，不展示验收编号或页面类型 |
| 指标数字 | 24px；1280宽度下22px；使用 tabular-nums 保持数字等宽 |
| 圆角 | 标准区块 10–12px；控件 6–8px |
| 间距 | 区块间16–20px；区块内12–16px；控件间8px |

金额保留原有“万元”口径，偏差显式显示正负；使用金额组件保留精度及完整金额提示。所有金额列应按页面逐步统一为右对齐。不要把所有金额和日期强制设为代码字体。

## 页面骨架

- 页面标题、面包屑和描述由 `PageHeader` 统一输出。页面清单与编号仍保留在路由清单中。
- 工作台、驾驶舱、项目详情使用浅灰工作区和独立业务卡片。其余业务页暂保留原有白色容器，按场景逐页迁移。
- 导航保留权限过滤，进入页面时展开当前业务组；具体项目的动态路由也须高亮对应入口。
- 最低桌面宽度为1280px，主要设计基准为1440×900。复杂表格允许局部横滚；页面整体不能横向溢出。

## 指标与状态

`MetricStatCard` 提供 card 与 flat 两种表现：独立指标使用 card，业务区块内的指标使用 flat，减少卡片嵌套。同排指标等高，辅助说明字号不得低于12px。

- 只有传入 onClick 的指标显示交互悬停态，支持 Enter 与空格触发，并有可见焦点。
- 红色表示高风险、严重、超期、超预算；橙黄表示关注或预警；绿色表示健康或完成。颜色必须伴随文字说明。
- 高风险标签使用红色，禁止落入默认绿色分支；风险标签不使用持续闪烁动画。
- 指标没有真实同比、环比或历史快照时，不填充虚构趋势。

## 三类页面模式

### 项目经理工作台

顺序：身份与范围 → 指标摘要 → 异常待办 / 临期里程碑与快捷发起 → 项目列表 → 验收与回款。

快捷发起默认显示采购、外包、费用，其他动作从“更多发起”进入；选择的项目必须带入所有原业务页面。项目列表保留完整业务字段及局部横滚。

### 经营驾驶舱

顺序：标题与工具 → 常用筛选 → 核心指标 → 重点异常与待决策 → 补充经营数据 → 分布与详细分析。

默认显示组织与健康度筛选，其余筛选可展开；收起时显示已选辅助条件数量，URL及下钻继续保留全部条件。“指标口径与常用视图”提供完整说明与保存视图能力。六种演示状态由页头“演示场景”入口控制。

### 项目详情

项目名称作为主标题，项目编码、部门、更新时间作为辅助信息。关键经营字段和健康原因直接展示；来源商机、组织与合同信息可展开。

采用两级页签，保留旧 tab 参数：

| 业务组 | 原有页面内容 |
|---|---|
| 项目总览 | 项目概览、团队、操作记录 |
| 计划与交付 | 计划进度、质量、交付物 |
| 成本与四算 | 四算、成本、采购外包、变更 |
| 协作事项 | 日报周报、问题风险、需求BUG |
| 验收与回款 | 验收结算、回款 |

业务组由当前 URL 中的 tab 自动确定。摘要点击、旧深链接、浏览器返回及切换项目须同步定位到正确分组，不能只变更视觉选中态。

## 验证与后续扩展

每轮检查1440与1280两种桌面宽度，查看初态、主要展开态、空态和权限状态，并验证核心下钻与返回。公共组件变更需要回归列表、表单、抽屉及权限场景。

本轮不引入图表库。四算专题的差额可视化、商机台账默认列优化和其余页面的容器迁移属于后续页面迭代，不能将此文档描述当作已经完成的实现。
