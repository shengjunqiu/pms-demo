# CF-07 后续边界（未正式验收）

当前代码已接入动作按钮、表单与确认，以及人员费率、联系方式、后评价编辑限制。概算人工单价遗漏已在 028cc34 修复；禁止改单价时仍可改数量与说明，新增/改编号/改科目不能用同一人工行绕过检查。

仍需实现原文 PT-047：

- 收入与成本独立的查看/编辑字段类别，当前 SensitiveField 仅 labor-rate/margin/evaluation/contact。
- 字段规则按目标项目/商机所属组织生效；现有组织范围只限制对象访问，字段判定仍按角色统一选策略。
- 新契约由协调者单写 models/configuration-access、mock/configuration-access、mock/access、hook；统一同步点后再按独占页面分包接入。涵盖商机预计收入/初评成本、概算收入/成本、预算采购外包等金额及其摘要/导出/审计消费者。
- 保持毛利为计算结果只读。查看与编辑分开；未授权旧字段必须保留原值，默认角色业务流程不能变成无权限。
- 新增定向浏览器场景：同角色组织 A 可编辑、组织 B 只读；收入禁编而成本可编；隐藏字段保存不清空旧值；权限恢复不重写历史。

正式计数仍 30/72，无活动轮次。并发包交付与回归不是页面 accept；CF-07 不得以本批部分实现直接验收。最终项目还需剩余页面及 FINAL。
